const express = require("express");

const todoController = require("./../controllers/todo");

const router = express.Router();

// router.post("/api/create", todoController.create);


module.exports = router;
